// الكود المسؤول عن التعدين، على سبيل المثال، هنا يتم استخدام عملية حسابية بسيطة كبديل عن التعدين الفعلي.
function mine() {
    let progress = 0;
    const totalIterations = 1000000; // عدد التكرارات التي يتم فيها التعدين (يمكنك تعديله حسب الحاجة)
  
    // عملية التعدين
    for (let i = 0; i < totalIterations; i++) {
      // مثال على عملية حسابية ثقيلة
      let hash = Math.pow(i, 2); // هذه عملية حسابية ثقيلة على سبيل المثال
      if (i % 10000 === 0) { // كل 10000 تكرار نرسل التقدم
        postMessage(i / totalIterations * 100); // إرسال التقدم الحالي إلى الصفحة الرئيسية
      }
    }
  
    // عند اكتمال التعدين
    postMessage('MiningComplete');
  }
  
  // استقبال الرسائل من الصفحة الرئيسية
  onmessage = function(e) {
    if (e.data === 'startMining') {
      mine(); // بدء التعدين عند تلقي الأمر
    }
  };
  